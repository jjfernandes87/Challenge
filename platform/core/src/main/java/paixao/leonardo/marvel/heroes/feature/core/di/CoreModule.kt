package paixao.leonardo.marvel.heroes.feature.core.di

import org.kodein.di.DI

object CoreModule {
    private const val MODULE_NAME = "MarvelCore"

    val injections = DI.Module(name = MODULE_NAME) {
    }
}
